the quick brown fox jumps over the lazy dog
the cat sat on the mat and the dog sat on the log
tokenization splits text into subword units before the model sees it
byte pair encoding merges the most frequent adjacent pair repeatedly
a large language model predicts the probability of the next token
attention lets each token attend to the other tokens in the sequence
the transformer stacks attention and a feed forward network many times
prefill processes the prompt in parallel and decode emits one token at a time
the key value cache avoids recomputing the earlier keys and values
temperature top k and top p control how the next token is sampled
안녕하세요 반갑습니다 오늘 날씨가 좋네요
고양이가 매트 위에 앉았다 그리고 강아지가 통나무 위에 앉았다
토크나이제이션은 텍스트를 서브워드 단위로 나눈다
바이트 페어 인코딩은 가장 자주 붙는 쌍을 반복해서 병합한다
대규모 언어 모델은 다음 토큰의 확률 분포를 예측한다
어텐션은 각 토큰이 다른 토큰을 참조하도록 문맥을 섞는다
트랜스포머는 어텐션과 피드포워드 네트워크를 여러 번 쌓는다
프리필은 프롬프트를 병렬로 처리하고 디코드는 토큰을 하나씩 생성한다
케이 밸류 캐시는 앞선 키와 값을 다시 계산하지 않게 저장한다
온도와 탑케이와 탑피는 다음 토큰을 고르는 방식을 통제한다
the gateway counts tokens estimates cost and manages the context window
streaming sends server sent events so the client renders tokens as they arrive
게이트웨이는 토큰을 세고 비용을 추정하고 컨텍스트 윈도우를 관리한다
스트리밍은 서버 센트 이벤트로 토큰이 도착하는 대로 클라이언트에 렌더링한다
